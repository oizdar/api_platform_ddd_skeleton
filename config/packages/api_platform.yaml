api_platform:
  mapping:
    paths: [ '%kernel.project_dir%/config/resources' ]
  patch_formats:
    json: [ 'application/merge-patch+json' ]
  swagger:
    versions: [ 3 ]
    api_keys:
      test:
        type: query
        name: json_login

